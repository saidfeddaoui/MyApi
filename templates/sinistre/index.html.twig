{% extends 'base.html.twig' %}
{% block javascripts %}
    {{ parent() }}
    <!-- BEGIN PAGE LEVEL SCRIPTS -->
    <script src="{{ asset('js/sinistre.js') }}" type="text/javascript"></script>
    <!-- END PAGE LEVEL SCRIPTS -->
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-12">
            <!-- BEGIN EXAMPLE TABLE PORTLET-->
            <div class="portlet light portlet-fit ">
                <div class="portlet-title">
                    <div class="caption">
                        <i class="icon-settings font-red"></i>
                        <span class="caption-subject font-red sbold uppercase">Gestion Des Sinistres</span>
                    </div>
                </div>
                <div class="portlet-body">
                    <div class="table-toolbar">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="btn-group">
                                    <button id="add-slider-btn" class="btn green" data-toggle="modal" href="#add-sinistre-modal"> Ajouter un type
                                        <i class="fa fa-plus"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <table class="table table-striped table-hover table-bordered datatable" id="sample_editable_1">
                        <thead>
                        <tr>
                            <th> Titre </th>
                            <th> العنوان </th>
                            <th> Icône </th>
                            <th> Icône(active) </th>
                            <th> Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for type in types %}
                            <tr data-id="{{ type.id }}">
                                <td> {{ type.title }} </td>
                                <td> {{ type.title_ar }} </td>
                                <td>
                                    {% if type.image %}
                                        <div >
                                            <img src="{{ asset('img/' ~ type.image.path) }}" class="product_img">
                                        </div>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if type.image_active %}
                                        <div >
                                            <img src="{{ asset('img/' ~ type.image_active.path) }}" class="product_img">
                                        </div>
                                    {% endif %}
                                </td>
                                <td>
                                    <a class="btn btn-xs btn-primary bs-tooltip edit" data-id= "{{type.id}}" data-placement="top" data-original-title="Editer"><i class="glyphicon glyphicon-edit" title="Modifier"></i></a>
                                    <a class="btn btn-xs btn-danger bs-tooltip delete" data-id= "{{type.id}}" data-placement="top" data-original-title="Supprimer"><i class="glyphicon glyphicon-trash" title="Supprimer"></i></a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- END EXAMPLE TABLE PORTLET-->
        </div>
    </div>
    <div class="modal fade " tabindex="1" role="dialog" id="add-sinistre-modal" data-backdrop="false">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
                    <h4 class="modal-title" id="myModalLabel">Ajouter un type sinistre : </h4>
                </div>
                <div class="modal-body">
                    <div class="form">{% include 'sinistre/form.html.twig' with {'form': form} %}</div>
                </div>
            </div><!-- /.modal-content -->
        </div><!-- /.modal-dialog -->
    </div><!-- /.modal -->
    <div class="modal fade " tabindex="1" role="dialog" id="edit-typeSinistre-modal" data-backdrop="false">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
                    <h4 class="modal-title" id="myModalLabel">Modifier ce type sinistre:</h4>
                </div>
                <div class="modal-body"><div class="form"></div></div>
            </div><!-- /.modal-content -->
        </div><!-- /.modal-dialog -->
    </div><!-- /.modal -->
{% endblock %}