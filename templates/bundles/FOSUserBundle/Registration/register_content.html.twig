{% trans_default_domain 'FOSUserBundle' %}

<div class="row">
    <div class="col-md-12">
        <!-- BEGIN EXAMPLE TABLE PORTLET-->
        <div class="portlet light portlet-fit ">
            <div class="portlet-title">
                <div class="caption">
                    <i class="icon-settings font-red"></i>
                    <span class="caption-subject font-red sbold uppercase">Ajouter un nouveau utilisateur</span>
                </div>
                <div class="tools">
                    <a href="javascript:;" class="expand" data-original-title="" title=""></a>
                </div>
            </div>
            <div class="portlet-body" style="display: none;">
                <div class="form">{% include '@FOSUser/Registration/form.html.twig' with {'form': form} %}</div>
            </div>
        </div>
        <!-- END EXAMPLE TABLE PORTLET-->
    </div>

</div>

<div class="row">
    <div class="col-md-12">
        <!-- BEGIN EXAMPLE TABLE PORTLET-->
        <div class="portlet light ">
            <div class="portlet-title">
                <div class="caption font-dark">
                    <i class="icon-settings font-dark"></i>
                    <span class="caption-subject bold uppercase"> Liste des utilisateurs</span>
                </div>
            </div>
            <div class="portlet-body">
                <table class="table table-striped table-bordered table-hover table-checkable order-column datatable" id="sample_1">
                    <thead>
                    <tr>
                        <th> Utilisateur </th>
                        <th> Email </th>
                        <th> Rôle </th>
                        <th> Actions </th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for key, user in users %}
                        <tr>
                            <td>{{ user.username }}</td>
                            <td>{{ user.email }}</td>
                            <td class="hidden-xs hidden-sm">{{ user.group.name }}</td>
                            <td>
                                <a href="{{ path('edit_user',{'id':user.id})}}" class="btn btn-xs btn-primary bs-tooltip" data-placement="top" data-original-title="Editer"><i class="glyphicon glyphicon-edit"></i></a>
                                <a class="btn btn-xs btn-danger bs-tooltip remove" data-id= "{{user.id}}" data-placement="top" data-original-title="Supprimer"><i class="glyphicon glyphicon-trash"></i></a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        <!-- END EXAMPLE TABLE PORTLET-->
    </div>
</div>