<div class="row">
    <div class="col-md-12">

        <ul class="nav nav-tabs">
            <li class="active">
                <a href="#informations_tab" data-toggle="tab">Informations Génarales</a>
            </li>
            <li>
                <a href="#circonstances_tab" data-toggle="tab">Circonstances</a>
            </li>

            <li>
                <a href="#dommages_tab" data-toggle="tab">Dommages au véhicule</a>
            </li>
        </ul>

        <div class="tab-content">
            <div class="tab-pane active" id="informations_tab">
                <h4> <i class="fa fa-user font-{{ app.session.get('insuranceType').themeColor }}"></i> {{ preDeclaration.contrat.client.firstName }} {{ preDeclaration.contrat.client.familyName }}</h4>
                <p>{{ preDeclaration.circumstance.adress }}</p>
                {#<dd>{{ preDeclaration.circumstance.latitude }}</dd>
                <dd>{{ preDeclaration.circumstance.longitude }}</dd>#}
                <p>Type de sinistre : {{ preDeclaration.typeSinistre.title }}</p>
                <p>Nombre de véhicules : {{ preDeclaration.nbVehicule }}</p>
                <p>Nombre de blessées : {{ preDeclaration.nbInjured }}</p>
                {% if preDeclaration.status == constant('App\\Entity\\PreDeclaration::STATUS_REJECTED') %}
                    <p>Raison de rejet : {{ preDeclaration.description }}</p>
                {% endif %}

            </div>

            <div class="tab-pane" id="circonstances_tab">
                <h4 style="margin-bottom: 15px">Circonstances .</h4>
                <ul class="nav nav-tabs">
                    <li class="active">
                        <a href="#localisation_tab" data-toggle="tab">Localisation</a>
                    </li>
                    {% if attachements %}
                        {% for photo in attachements %}
                            <li>
                                <a href="#photo_{{ photo.id }}_tab" data-toggle="tab"> Photo {{ loop.index }} </a>
                            </li>
                        {% endfor %}
                    {% endif %}
                </ul>

                <div class="tab-content">
                    <div class="tab-pane active" id="localisation_tab">
                        <h4>An example implementing a preview pane.</h4>
                        <p> Obviously the most visual demo, the preview pane is accomplished entirely outside of Jcrop with a simple jQuery-flavored callback. This type of interface could be useful for creating a thumbnail or avatar.
                            The onChange event handler is used to update the view in the preview pane. </p>
                    </div>
                    {% if attachements %}
                        {% for photo in attachements %}
                            <div class="tab-pane" id="photo_{{ photo.id }}_tab">
                                <h4>Photo {{ loop.index }}</h4>
                                <div class="row">
                                    <div class="col-md-6 responsive-1024">
                                        <img src="{{ asset('img/tiers/' ~ photo.path) }}" id="photo-attachment-circumstance-{{ loop.index }}" alt="Photo circonstance"/>
                                    </div>
                                    {#  <div class="col-md-6 responsive-1024">
                                          <div id="preview-pane" class="preview-photo-attachment-circumstance-{{ loop.index }}">
                                              <div class="preview-container">
                                                  <img src="{{ asset('img/tiers/' ~ photo.path) }}" class="jcrop-preview" alt="Preview" />
                                              </div>
                                          </div>
                                      </div> #}
                                </div>
                            </div>
                        {% endfor %}
                    {% endif %}
                </div>


            </div>

            <div class="tab-pane" id="dommages_tab">
                <h4>Dommages au véhicule</h4>
                {% if preDeclaration.vehiculeDamage %}
                <div class="row">
                    <div class="col-md-4"></div>
                    <div class="col-md-4">
                        <div style="background-image: url('{{ asset('img/vehicle_zones/0.png') }}'); background-repeat: no-repeat;">
                            {% for part in preDeclaration.vehiculeDamage.damagedParts %}
                                <img {% if not loop.first %} class="vehicle-zone" {% endif %}
                                        src="{{ asset('img/vehicle_zones/' ~ part.code ~ '.png') }}"
                                />
                            {% endfor %}
                        </div>
                    </div>
                    <div class="col-md-4"></div>


                {% endif %}
            </div>

        </div>



    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <div class="pull-right">
            <a href="{{ path('pre_declarations_in_progress') }}" class="btn dark btn-dark">Retour</a>
        {% if preDeclaration.status == constant('App\\Entity\\PreDeclaration::STATUS_IN_PROGRESS') %}
            <button type="button" class="btn red reject" data-id= "{{ preDeclaration.id }}" >Rejeter</button>
            <button type="button" class="btn green accept" data-id= "{{ preDeclaration.id }}" >Accepter</button>
        {% endif %}
        </div>
    </div>
</div>

</div>